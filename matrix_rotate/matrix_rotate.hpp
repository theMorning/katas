#ifndef matrix_rotate_h_
#define matrix_rotate_h_

#include <vector>
#include <iostream>

using std::vector;

void matrix_rotate(vector<vector<int>> &matrix);
void print_matrix(vector<vector<int>> matrix, std::ostream& cout = std::cout);

#endif
